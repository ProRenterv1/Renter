{% if user %}Hi {{ user.get_full_name|default:user.username }},{% else %}Hi,{% endif %}

This is a confirmation that your password was just updated.

If you did not make this change, reset your password immediately or contact support.
