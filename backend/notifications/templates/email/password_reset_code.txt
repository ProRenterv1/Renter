{% if user %}Hi {{ user.get_full_name|default:user.username }},{% else %}Hi,{% endif %}

Use the following code to reset your password:

    {{ code }}

The code expires shortly. If you did not request a reset you can safely ignore this email.
